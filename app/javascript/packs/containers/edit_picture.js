import React from 'react';
import PropTypes from 'prop-types';

class EditPictureContainer extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        MyComponent
      </div>
    );
  }
}

EditPictureContainer.propTypes = {
};

export default EditPictureContainer;
