%tr.with-row-select
  %td
    %a{href: edit_director_size_path(size)}
      = size.name
  %td
    = size.description
  %td
    = link_to "#{icon('trash')}".html_safe, director_size_path(size), method: :delete, confirm: 'Are you sure ?'