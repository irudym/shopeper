= section_menu(@menu)
= show_notice(notice)
.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Size
          %th Description
          %th Restore
          %th Delete
      %tbody
        = render partial: "size_restore", collection: @sizes