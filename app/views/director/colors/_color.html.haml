%tr.with-row-select
  %td
    %a{href: edit_director_color_path(color)}
      = color_box(color.hex_code)
  %td
    %a{href: edit_director_color_path(color)}
      = color.name
  %td
    = link_to "#{icon('trash')}".html_safe, director_color_path(color), method: :delete, confirm: 'Are you sure ?'