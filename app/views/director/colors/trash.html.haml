= section_menu(@menu)
= show_notice(notice)
.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Color
          %th Name
          %th Restore
          %th Delete
      %tbody
        = render partial: "color_restore", collection: @colors