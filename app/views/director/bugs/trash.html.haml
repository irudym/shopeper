= section_menu(@menu)
= show_notice(notice)
.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Owned
          %th Title
          %th Comments
          %th Restore
          %th Delete
      %tbody
        = render partial: "bug_restore", collection: @bugs
