= section_menu(@menu)
= show_notice(notice)
.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Name
          %th Address
          %th Description
          %th Brands
          %th
      %tbody
        = render partial: "shop", collection: @shops