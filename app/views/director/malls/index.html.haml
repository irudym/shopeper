= section_menu(@menu)
= show_notice(notice)
.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Name
          %th Address
          %th Description
          %th Delete
      %tbody
        = render partial: "mall", collection: @malls
